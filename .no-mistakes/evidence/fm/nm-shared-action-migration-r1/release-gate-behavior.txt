=== structural release-please PR through release caller contract ===
PR #78 is a release-please release PR (same-repo branch 'release-please--branches--main' with the Release Please footer); exempt from the no-mistakes signature.
exit_status=0

=== bot identity suppressed and no qualifying body ===
::error::This PR was not raised through no-mistakes.

Contributions to this repository must be submitted via 'git push no-mistakes'.
That pipeline runs the required review/test/lint/CI steps and writes a
deterministic '## Pipeline' section into the PR body containing:

    Updates from [git push no-mistakes](https://github.com/kunchenguid/no-mistakes)

The only other way to pass is release-please's own release PR, which must
satisfy all three structural conditions: a 'release-please--'
(or legacy 'release-please/') head branch, a
same-repository (non-fork) head, and the Release Please body footer.

PR author: release-workflow-sentinel
Head branch: release-please--branches--main
Head repository: kunchenguid/treehouse (base kunchenguid/treehouse)
exit_status=1

=== attestation body cannot pass because release caller omits PR_HEAD_SHA ===
::error::This PR's no-mistakes pipeline attestation is not bound to the current pull request head.

The attestation head_sha must equal the current pull request head SHA so a later push cannot pass on a stale attestation.
  attestation head_sha: 0123456789abcdef0123456789abcdef01234567
  pull request head:    (missing)

Re-run 'git push no-mistakes' on the current head so the PR body attestation is rewritten for this commit.

PR author: release-workflow-sentinel
exit_status=1

